/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['your-domain.com', 'res.cloudinary.com'], // ← Add your image source domains here
  },
};

export default nextConfig;
